# Prompt reutilizável — configurar Jira

Cole esta mensagem numa conversa nova (separada do `/qa-curso`), trocando só o trecho
`[PERGUNTA]` pelo que você precisa resolver naquele momento.

---

Quero configurar/ajustar o **Jira** para apoiar meu trabalho de QA no projeto
**finance-api**. Não existe conector MCP pronto para o Jira, então a configuração é
manual — me guie passo a passo pela interface.

**Contexto do projeto:**
- Projeto: `FIN — Finance API` (chave `FIN`)
- Uso o Jira para rastrear stories e bugs encontrados durante a prática de QA
  (`qa-course/modulos/03-jira-qase.md`)
- Workflow de bug esperado: `New → Open → In Progress → Ready for Retest → Closed`,
  com `Reopened`, `Won't Fix`, `Duplicate` e `Cannot Reproduce` como desvios
- Board: `To Do → In Progress → In QA → Done`

**O que já está pronto:** *(preencher a cada uso — ex.: "projeto criado, tipos de issue
ok, falta configurar o workflow")*

**O que preciso agora:**
[PERGUNTA]
